import { MyConvertCasePipe } from './my-convert-case.pipe';

describe('MyConvertCasePipe', () => {
  it('create an instance', () => {
    const pipe = new MyConvertCasePipe();
    expect(pipe).toBeTruthy();
  });
});
